export interface Modal_states {
  genre: boolean;
  year: boolean;
}

export interface Filters {
  adult: boolean;
  video: boolean;
  language: string;
  page: number;
  sort: string;
  genres: string;
  year: number;
}
